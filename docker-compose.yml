version: '3.3'

services:
  aic_nginx:
    image: nginx:latest
    container_name: aic_nginx_cont
    ports:
      - 2018:2018
      - 80:80
      - 443:443
    volumes:
      - .:/aic_site
      - ./config/nginx/sites-enabled:/etc/nginx/sites-enabled
      - ./config/nginx/sites-available:/etc/nginx/sites-available
      - ./config/nginx/nginx.conf:/etc/nginx/nginx.conf

    depends_on:
      - aic_web
  aic_web:
    build: .
    container_name: aic_web_cont
    command: bash -c "cd .. && python3 manage.py makemigrations && python3 manage.py migrate && gunicorn aic_site.wsgi -b 0.0.0.0:8000"
    depends_on:
      - aic_db
    expose:
      - 8000
  aic_db:
    image: postgres:latest
    container_name: aic_db_cont
